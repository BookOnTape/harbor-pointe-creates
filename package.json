{
  "name": "harbor-pointe-creates",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "astro build && wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply harbor-pointe-creates --local",
    "db:migrate": "wrangler d1 migrations apply harbor-pointe-creates --remote",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
